# Sao chép file này thành .env.local (không commit .env.local lên git) và
# điền giá trị thật cho môi trường local/staging/production.

# Base URL của backend API. apiClient.ts đọc biến này qua import.meta.env.
VITE_API_BASE_URL=http://localhost:8080/api
